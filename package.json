{
  "name": "task-management",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install --prefix server && npm run generate --prefix server",
    "postinstall": "npm install --prefix server"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  }
}
