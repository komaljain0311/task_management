{
  "name": "task-management",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install --prefix server && npx prisma generate --schema=server/prisma/schema.prisma",
    "postinstall": "npm install --prefix server"
  }
}
