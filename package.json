{
  "name": "task-management",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install --prefix server && npm run generate --prefix server && npm install --prefix client && npm run build --prefix client",
    "postinstall": "npm install --prefix server && npm install --prefix client",
    "test-build": "npm run build"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  }
}
