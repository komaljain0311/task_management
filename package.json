{
  "name": "task-management",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npx prisma generate --schema=server/prisma/schema.prisma && npm run build --prefix client",
    "postinstall": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  }
}
